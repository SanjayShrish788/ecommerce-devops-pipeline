import java.security.MessageDigest;
import java.util.Base64;
public class Test {
    public static void main(String[] args) {
        // BCryptPasswordEncoder is not in default java, let's just make the app do it on startup!
    }
}
